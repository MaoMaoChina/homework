<h1 align="center">
    <body bgcolor="gold">
    @if($length==0)
        Sorry, the data is null.
        @else
        The size of the data is &nbsp;{{ $length}}
        @endif
    <div class="links">
        <a href="http://localhost/demo/public/">return main menu</a>
    </div>
    </body>
</h1>